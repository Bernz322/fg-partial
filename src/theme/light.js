import {
  green,
  teal,
} from "@mui/material/colors";

export const lightTheme = {
  palette: {
    mode: "light",
    primary: {
      main: green[500],
    },
    secondary: {
      main: teal[500],
    },
  },
};
