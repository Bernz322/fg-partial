import {
  green,
  teal,
} from "@mui/material/colors";

export const darkTheme = {
  palette: {
    mode: "dark",
    primary: {
      main: green[300],
    },
    secondary: {
      main: teal[300],
    },
  },
};
